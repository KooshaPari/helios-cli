# Network proxy

This component provides the network proxy surface for the `codex-rs` workspace.

## Guidance

- Keep the docs local to this component.
- Describe the current proxy behavior and config here.
- Avoid leaving merge conflict markers in the live docs.

